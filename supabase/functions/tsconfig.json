{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "lib": ["ES2022", "DOM"],
    "types": ["vitest/globals"],
    "declaration": false,
    "declarationMap": false,
    "resolveJsonModule": true,
    "noEmit": true,
    "allowImportingTsExtensions": true
  },
  // index.ts (the actual deployed Edge Function entrypoint) is included and
  // typechecked here, same as every other file in this package — it is NOT
  // excluded. Its `jsr:` import and `Deno.*` globals are made checkable via
  // the ambient stubs in embed-coaching-note/deno.d.ts and
  // embed-coaching-note/jsr-supabase.d.ts (picked up by the "**/*.d.ts"
  // glob below), which is why `noEmit` and `allowImportingTsExtensions` are
  // needed: this package's own `.ts`-suffixed relative import
  // (`./_requestShape.ts`) is otherwise a tsc error, and
  // `allowImportingTsExtensions` requires `noEmit` (or
  // `emitDeclarationOnly`) — a no-op here since `pnpm run typecheck` already
  // invokes `tsc --noEmit`.
  "include": ["**/_requestShape.ts", "**/_auth.ts", "**/*.test.ts", "**/index.ts", "**/*.d.ts"]
}
