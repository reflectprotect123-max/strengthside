---
alwaysApply: true
description: Strength and Engine are two products and two repos
---

# Two products, two repos

Strength is this repo (`apps/athlete/`). The Engine is a **sibling GitHub repo**
(`the-engine`), same as `nutrition`.

Do not add `apps/engine/`, `packages/adaptive/`, or `apps/hybrid-engine/`.
Do not mix Engine splits/watts/RPM into the Strength TRACK logger.
---
