{
  "name": "@hybrid/engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "//": "Exports TypeScript source directly. Every consumer (Vite, Vitest, Metro) compiles TS itself, so shipping source removes a build-ordering step between packages and keeps go-to-definition landing on the real code.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./golden/*": "./test/golden/*"
  },
  "dependencies": {
    "@hybrid/shared-core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
