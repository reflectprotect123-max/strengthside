# Nutrition archive (not product)

Extracted from git commit `c6462ae` (parent of blank-slate delete `29bfa8e`).

**Not wired into the athlete app.** Do not move these files back under `packages/` or `apps/mobile/prototype/hybrid-app/` without an explicit product decision. `pnpm` workspace and `verify` must ignore this tree.

Contents: `nutrition-core`, `nutrition-engine`, and the old HTML nutrition UI/sync/bundle + smokes.

