{
  "name": "the-strength-engine",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm --filter @hybrid/strength-engine build",
    "check:migrations": "node checks/migrations-apply.mjs",
    "check:engine-adapter": "node apps/mobile/prototype/hybrid-app/engine-adapter.parity.mjs",
    "check:label-scan": "node apps/mobile/prototype/hybrid-app/label-scan.smoke.mjs",
    "check:native-bridge": "node apps/mobile/prototype/hybrid-app/native-bridge.barcode.smoke.mjs",
    "check:nutrition-ui": "node apps/mobile/prototype/hybrid-app/nutrition-ui.smoke.mjs && node apps/mobile/prototype/hybrid-app/nutrition-ui.barcode.smoke.mjs",
    "check:food-catalog": "node apps/mobile/prototype/hybrid-app/food-catalog.smoke.mjs",
    "check:hybrid-proxy": "bash apps/mobile/sync-hybrid-html.sh && node apps/mobile/prototype/hybrid-app/hybrid-proxy.smoke.mjs",
    "//verify": "Everything CI runs. The two lists SHOULD agree \u2014 a check that runs in CI and not here sits red on main while every local run says green, which the hybrid repo paid for on 16 August 2026. If they ever have to differ, write the reason next to the difference.",
    "verify": "pnpm run typecheck && pnpm run test && pnpm run check:migrations && pnpm run build && pnpm run check:engine-adapter && pnpm run check:label-scan && pnpm run check:native-bridge && pnpm run check:nutrition-ui && pnpm run check:food-catalog && pnpm run check:hybrid-proxy"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
