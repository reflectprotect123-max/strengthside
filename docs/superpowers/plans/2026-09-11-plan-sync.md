# Plan sync (copy machine) Implementation Plan

> **For agentic workers:** implement TDD; do not restore deleted `StrengthSync`.

**Goal:** Offline-first copy of Strength Side Library templates + calendar/logger sessions to the shared Supabase snapshot RPC, so another signed-in phone can pull them. The Brain reads this later (`domain = strength_side`). No new Postgres table in this repo. WHOOP stays a separate button.

**Spec:** `docs/superpowers/specs/2026-09-05-session-template-sync-contract.md`

## Files
- `apps/athlete/plan-sync.js` — pack, rev, merge, push/pull, debounce
- `apps/athlete/plan-sync.test.js`
- Wire: `index.html`, `app.js` `save` / Me / boot, `service-worker.js`
- Smoke: `athlete-app.smoke.mjs` + `check:athlete-app`
