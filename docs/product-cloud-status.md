# Product cloud provision status

Generated by `scripts/provision-product-clouds.sh`. Secrets are never written here.

## Hybrid Strength

- Capgo: attempting `app add com.hybrid.strength` from product capacitor config
┌  Adding
│
●  Using the organization "hybrid engine" as the app owner
│
●  Adding com.hybrid.strength to Capgo
│
▲  Cannot find app icon in any of the following locations: resources/icon.png, assets/icon.png
│
◆  App com.hybrid.strength added to Capgo
│
●  This app is accessible to all members of your organization based on their permissions
│
●  Next step: upload a bundle with "npx @capgo/cli bundle upload com.hybrid.strength"
│
└  Done ✅

- Capgo: app add reported success for `com.hybrid.strength`
┌  Create channel
│
■  No capacitor config file found, run `cap init` first
│
■  No capacitor config file found, run `cap init` first
│
●  Creating channel com.hybrid.strength#dogfood to Capgo
│
◆  Channel created ✅
│
└  Done ✅

┌  Create channel
│
■  No capacitor config file found, run `cap init` first
│
■  No capacitor config file found, run `cap init` first
│
●  Creating channel com.hybrid.strength#live to Capgo
│
◆  Channel created ✅
│
└  Done ✅

- Netlify: skipped (no NETLIFY_AUTH_TOKEN). Create site `hybrid-strength`, add GitHub secret `NETLIFY_SITE_ID_STRENGTH`, workflow deploys on main.

## Hybrid Engine

- Capgo: attempting `app add com.hybrid.engine` from product capacitor config
┌  Adding
│
●  Using the organization "hybrid engine" as the app owner
│
●  Adding com.hybrid.engine to Capgo
│
▲  Cannot find app icon in any of the following locations: resources/icon.png, assets/icon.png
│
◆  App com.hybrid.engine added to Capgo
│
●  This app is accessible to all members of your organization based on their permissions
│
●  Next step: upload a bundle with "npx @capgo/cli bundle upload com.hybrid.engine"
│
└  Done ✅

- Capgo: app add reported success for `com.hybrid.engine`
┌  Create channel
│
■  No capacitor config file found, run `cap init` first
│
■  No capacitor config file found, run `cap init` first
│
●  Creating channel com.hybrid.engine#dogfood to Capgo
│
◆  Channel created ✅
│
└  Done ✅

┌  Create channel
│
■  No capacitor config file found, run `cap init` first
│
■  No capacitor config file found, run `cap init` first
│
●  Creating channel com.hybrid.engine#live to Capgo
│
◆  Channel created ✅
│
└  Done ✅

- Netlify: skipped (no NETLIFY_AUTH_TOKEN). Create site `hybrid-engine`, add GitHub secret `NETLIFY_SITE_ID_ENGINE`, workflow deploys on main.

GitHub repos, Play listings, and hybrid1 OAuth redirect maps cannot be created from this agent (gh is read-only; Play is manual; WHOOP callback host stays thehybridengine1).

Initial Capgo **bundle upload 1.0.0** for both new apps hit org-wide TUS `429 on_premise_app` (`retryAfterSeconds: 3600`). Apps and `dogfood`/`live` channels exist. Retry `PRODUCT=strength|engine CAPGO_BUNDLE_VERSION=1.0.0 bash scripts/ship-product-capgo.sh` after the window. Mixed `com.hybrid.athlete` was not uploaded.

