# THE Hybrid App (the athlete product)

Edit `index.html`, then from `apps/mobile`:

```bash
bash sync-hybrid-html.sh
```

Play `apps/mobile/THE-Hybrid-App.html` or serve this folder:

```bash
python3 -m http.server 4173
# http://localhost:4173
```

See `apps/mobile/PLAY.md`.
