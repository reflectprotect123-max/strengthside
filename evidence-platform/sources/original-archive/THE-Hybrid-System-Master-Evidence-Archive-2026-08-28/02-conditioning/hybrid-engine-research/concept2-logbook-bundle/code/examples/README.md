# Example fixture status

These JSON files are sanitized, documentation-shaped or mocked examples from
the research record. They are not captured from the user's Concept2 account and
must not be treated as a guarantee that every production result has the same
nesting or field types.

