# Athlete app. Functions + static site live under preview-site.
[build]
  base = "apps/mobile/preview-site"
  publish = "."

[functions]
  directory = "apps/mobile/preview-site/netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["@netlify/blobs"]
